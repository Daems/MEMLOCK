# Start Here · VIZIGROK Compaction

This repo is the Nano Banana Pro automation converted into a VIZIGROK compaction packet.

Read:

1. `CLAUDE.md`
2. `compaction/VIZIGROK_COMPACTION.md`
3. `compaction/VIZIGROK_PACKET.yaml`
4. `dashboards/vizigrok_compaction.html`

Then run the automation through the original CLI in `README.md`.

Core spell:

```text
prompt queue → Kie.ai task → callback/poll → download → SQLite log → review gate → publish/archive
```

Compaction doctrine:

```text
Markdown = handoff
YAML/JSON = packet
JSONL = trail
Mermaid = topology
HTML = cockpit
Source = execution truth
```
