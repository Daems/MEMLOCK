# Next Actions

## Immediate

1. Validate JSON syntax for schemas and fixtures.
2. Draft `CANON_MANIFEST_v0.1` using admitted source terms only.
3. Create first `VALIDATION_RUN_RECORD` for a dry Case A run.
4. Build a minimal source-span ledger for the v0.2 package.
5. Define graph-construction audit checks before relying on mechanical graph verification.

## Short sequence

### Step 1: Canon manifest extraction

Extract only source-bound terms from the method package:

- CSRS
- Canon Manifest
- Boundary Conditions
- Forensic Reseeding
- Burden-Aware Rehydration
- Adjudication Surface
- Method Failure Ledger
- driftlint
- Semantic Drift Rate
- Invariant Retention

### Step 2: Case A dry run

Use four baselines:

- raw long-context prompt
- naive summary prompt
- standard RAG prompt
- CSRS reseed plus rehydration prompt

Measure:

- single-fact retrieval accuracy
- multi-fact synthesis accuracy
- source-span support ratio
- unsupported claim ratio
- middle-context recovery rate
- rehydration burden cost
- verification latency

### Step 3: Case B split

Run B1 and B2 separately:

- B1: recursive degradation pressure
- B2: transfer and ontology pressure

### Step 4: Product wrapper rewrite

After initial validation records exist, rewrite the wrapper as:

- bounded architecture
- evidence ladder
- decision artifact product
- proof language only with proof artifacts
- operational claims only with run records

## Reserved work

Reserved until supporting artifacts exist:

- manifesto drafting
- mathematical proof posture
- production readiness posture
- standardization posture
- guaranteed elimination language
